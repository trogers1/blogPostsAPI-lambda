version: '3'

services:
  mongodb:
    image: mongo:3.4
    ports:
      - '27017:27017'
    networks:
      - dev
    depends_on:
      - mongoseed

  mongoseed:
    container_name: mongoseed
    build: ./config/mongoseed
    networks:
      - dev

networks:
  dev:
    driver: bridge
